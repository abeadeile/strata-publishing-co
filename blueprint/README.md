# Architecture contracts

These TypeScript files specify data shapes and queries for next build. They are design inputs, not a runnable application. Build model should move/refine them under `site/lib/` and add runtime validation before use.
